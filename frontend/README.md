# Chess Lab Frontend

Vue 3 + TypeScript client for the local Chess Lab app.

```powershell
npm install
npm run dev
npm run test
npm run build
```

Set `VITE_API_BASE_URL` if the API is not running at `http://127.0.0.1:8080`.
